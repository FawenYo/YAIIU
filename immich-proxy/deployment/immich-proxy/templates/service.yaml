apiVersion: v1
kind: Service
metadata:
  name: {{ include "immich-proxy.fullname" . }}
  labels:
    {{- include "immich-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "immich-proxy.selectorLabels" . | nindent 4 }}
